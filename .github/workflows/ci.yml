name: Publish JaCoCo to Pages

on:
  push:
    branches: ["tah-dev"]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "20"
          cache: "maven"

      - name: Build + Test (generate JaCoCo)
        run: |
          cd recipe-planner-backend
          mvn -B clean verify

      - name: Prepare Pages folder
        run: |
          rm -rf public
          mkdir -p public/jacoco
          cp -r recipe-planner-backend/target/site/jacoco/* public/jacoco/

          # Root page -> direkt zum Report
          cat > public/index.html <<'EOF'
          <!doctype html>
          <html>
            <head>
              <meta charset="utf-8"/>
              <meta http-equiv="refresh" content="0; url=./jacoco/index.html"/>
              <title>JaCoCo Report</title>
            </head>
            <body>
              <p>Redirecting to <a href="./jacoco/index.html">JaCoCo report</a>…</p>
            </body>
          </html>
          EOF

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: public

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
